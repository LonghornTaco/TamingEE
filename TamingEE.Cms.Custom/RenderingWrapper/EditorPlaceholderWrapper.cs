using Sitecore.Mvc.ExperienceEditor.Presentation;
using System.IO;

namespace TamingEE.Cms.Custom.RenderingWrapper
{
    public class EditorPlaceholderWrapper : Wrapper
    {
        public EditorPlaceholderWrapper(TextWriter writer, IMarker marker) : base(writer, marker)
        {
        }
    }
}
